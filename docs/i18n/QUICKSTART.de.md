# Quickstart

Fork of nexu-io/open-design (Apache-2.0).

See the English guide: [`../../QUICKSTART.md`](../../QUICKSTART.md).

Website: https://tryopencomputer.com

```bash
corepack enable
pnpm install
pnpm tools-dev run web
```

Then Settings → Inferno → paste API key. CLI is `ocd`.
